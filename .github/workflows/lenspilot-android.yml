name: LensPilot Android
on:
  push:
    paths: ['android/**', '.github/workflows/lenspilot-android.yml']
  pull_request:
    paths: ['android/**', '.github/workflows/lenspilot-android.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Build and lint Android app
        working-directory: android
        run: gradle --no-daemon :app:assembleDebug :app:lintDebug
      - name: Test local guidance rules
        working-directory: android
        run: |
          javac -d /tmp/lenspilot-guidance app/src/main/java/ai/lenspilot/android/GuidanceEngine.java tests/GuidanceEngineTest.java
          java -cp /tmp/lenspilot-guidance GuidanceEngineTest
      - uses: actions/upload-artifact@v4
        with:
          name: lenspilot-android-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
